import { CartPage } from "@/features/cart/cart-page";

export const metadata = {
  title: "Your Bag | Silver Looms",
  description: "Review your shopping bag and proceed to checkout.",
};

export default function Page() {
  return <CartPage />;
}
